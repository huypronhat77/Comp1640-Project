@model List<CourseWorkProject.Models.Comment>

@if(Model.Count != 0)
{
    foreach(var com in Model)
    {
        <div class="comment-box">
            <span class="user-comment">@com.User.Profile.Name</span><br/>
            <p>@com.Content</p>
        </div>
    }
}